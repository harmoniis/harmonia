# DEPRECATED: This WORKSPACE file is kept for backward compat with Bazel < 7.
# The authoritative build config is MODULE.bazel (Bzlmod).
# See MODULE.bazel for toolchain, crate universe, and platform config.
workspace(name = "harmonia")

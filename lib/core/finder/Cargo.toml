[package]
name = "harmonia-finder"
version = "0.1.9"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "fff-search-backed fuzzy file + content finder — the default local retrieval substrate over project and memory files"
keywords = ["harmonia", "search", "fuzzy", "grep", "finder"]
categories = ["filesystem"]

[lib]

[dependencies]
fff-search = "0.9"
harmonia-config-store = { version = "0.1.9", path = "../config-store" }
harmonia-actor-protocol = { version = "0.1.9", path = "../actor-protocol" }

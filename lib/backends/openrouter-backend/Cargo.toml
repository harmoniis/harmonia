[package]
name = "harmonia-openrouter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
harmonia-vault = { path = "../../core/vault" }
harmonia-config-store = { path = "../../core/config-store" }

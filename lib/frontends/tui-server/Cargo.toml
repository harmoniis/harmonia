[package]
name = "harmonia-tui-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "TUI server-side transports (UDS local, tailnet remote) for the Harmonia agent"
keywords = ["harmonia", "tui"]

[lib]

[dependencies]
harmonia-config-store = { version = "0.1.9", path = "../../core/config-store" }
harmonia-frontend-trait = { version = "0.1.0", path = "../shared/frontend-trait" }
async-trait = "0.1"
libc = "0.2"
dirs = "5"

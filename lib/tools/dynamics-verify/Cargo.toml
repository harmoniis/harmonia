[package]
name = "harmonia-dynamics-verify"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Formal verification of Harmonia's chaotic substrate (Lyapunov/F1/F2) + D1 symbolic recovery. Pure Rust, no deps; integrators copied verbatim from cited source lines. Replaces the deleted Python — Lisp+Rust only."
keywords = ["harmonia", "dynamics", "verification"]
categories = ["science"]

[[bin]]
name = "dynamics-verify"
path = "src/main.rs"

[dependencies]
